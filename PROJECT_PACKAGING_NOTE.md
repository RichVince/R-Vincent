# Richard Vincent GRC Portfolio Package

Use `index.html` as the repository homepage and `grc-projects.html` as the complete project library. Upload the complete `assets/grc-project-pdfs/` folder because every project button now opens a browser-viewable PDF. Editable Word and Excel originals remain in `assets/grc-projects/` for private editing or optional download use.

The project files in `assets/grc-projects/` are portfolio evidence. The Phone Boss ULTD documents are owner-led internal governance artifacts and self-assessments. They do not represent ISO 27001 certification, a certification audit, or an official NIST maturity score.

The current résumé upload was a Microsoft Word temporary lock file and must be replaced with the actual `.docx` before the résumé can be updated. Resume links have been temporarily removed from `index.html` so the published site does not produce a 404 error.
